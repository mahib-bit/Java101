const person = {
    name: "Hero alom",
    age: 24,
    friends: ["bd", "test"],
    country: "bangladesh"
}
const { name, country, age, friends } = person;
console.log(friends);

//Don't have to write person.name, person.country, person.age, person.friends